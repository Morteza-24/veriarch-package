# Core (required)
numpy>=1.24

# Repository parsing
javalang>=0.13.0

# Semantic embeddings (falls back to scikit-learn TF-IDF+SVD if unavailable)
torch>=2.0
transformers>=4.30
scikit-learn>=1.2

# LLM backends
anthropic>=0.34      # only needed if --llm-provider anthropic
requests>=2.31        # needed for ollama / openai_compatible providers

# Web GUI (server.py)
fastapi>=0.110
uvicorn>=0.29
pydantic>=2.0
